from django.contrib import admin
from .models import new_Plant,new_Gateway,new_Device

admin.site.register(new_Plant)
admin.site.register(new_Gateway)
admin.site.register(new_Device)

# Register your models here.
